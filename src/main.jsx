import React from "react";
import ReactDOM from "react-dom/client";
import SaloniPreview from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SaloniPreview />
  </React.StrictMode>
);
